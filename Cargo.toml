[workspace]
resolver = "1"
members = [
    "core/compiler",
    "core/runtime",
    "core/common",
    "core/atlas",
]
exclude = [
    "examples"
]

[profile.bench]
opt-level = 3
lto = true
codegen-units = 1
overflow-checks = false
