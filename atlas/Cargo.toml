[package]
name = "atlas"
version = "0.3.5"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
# Maybe I should link the repository directly
compiler = { path = "../compiler" }